{
  "expectations": [
    {"text": "Uses `herdr worktree create` or `herdr worktree open` rather than `herdr workspace create --cwd`", "passed": true, "evidence": "herdr worktree create \\ --workspace w8 \\ --branch nexus3/buildkit-poisoned-snapshot \\ --base develop"},
    {"text": "Reads workspace id and worktree path from the command's JSON output instead of predicting or hardcoding them", "passed": false, "evidence": "Checkout path is hardcoded: 'git -C /home/newman/.herdr/worktrees/nexus3/buildkit-poisoned-snapshot \\ log --oneline develop..HEAD'. The worktree path is predicted rather than read from the create command's JSON output."},
    {"text": "Verifies the workspace shows is_linked_worktree=true before proceeding", "passed": true, "evidence": "new entry must show `worktree.is_linked_worktree = true`. If it does not, the workspace was created wrong"},
    {"text": "Recognises that the auto-provision hook binds the sandbox, rather than racing or fighting it", "passed": true, "evidence": "The auto-provision hook fires on workspace create and binds a sandbox automatically. Do not race it."},
    {"text": "Points the in-guest agent at .groundwork via an absolute host path, not a /workspace-relative one", "passed": true, "evidence": "The motive charter is at /home/newman/magic/nexus3/.groundwork/motives/nexus3-buildkit-poisoned-snapshot-on-vm-death/motive.md — read it first"},
    {"text": "Brief instructs the agent to install build-essential before building", "passed": true, "evidence": "The guest has no make/gcc by default: run 'apt-get install -y build-essential' first."},
    {"text": "Contains no hardcoded /home/<user> paths", "passed": false, "evidence": "Two hardcoded user paths: 'git -C /home/newman/.herdr/worktrees/nexus3/buildkit-poisoned-snapshot' (step 4 command user runs) and '/home/newman/magic/nexus3/.groundwork/' in the agent brief — both would break for a different user."}
  ]
}
